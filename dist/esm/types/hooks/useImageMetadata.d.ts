import { MediaMetadataHookInput, MediaMetadataHookOutput } from './sharedTypes';
export declare const useImageMetadata: ({ userId, roomId, h5pId, h5pSubId, client, }: MediaMetadataHookInput) => MediaMetadataHookOutput;
